tenseal==0.3.16
numpy==2.3.5
phe==1.5.0
cryptography==43.0.3
# pyfhel is not included because it typically requires Visual Studio + Windows SDK to build from source.
# If you need pyfhel, see the README troubleshooting section for guidance.
